package com.alexrotariu.finkeepy.ui

import com.alexrotariu.finkeepy.R

enum class ValueType(val labelResource: Int) {
    NET_WORTH(R.string.net_worth),
    INCOME(R.string.income),
    EXPENSE(R.string.expense),
    CASHFLOW(R.string.cashflow)
}
